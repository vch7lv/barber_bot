services:
  barber_bot:
    build: .
    restart: unless-stopped
    env_file: .env
    environment:
      - DATABASE_DSN=file:/app/data/barber_bot.db?_journal_mode=WAL
      - BACKUP_DIR=/app/backups
    volumes:
      - ./data:/app/data
      - ./backups:/app/backups
